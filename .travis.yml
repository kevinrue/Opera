sudo: false

language: node_js
node_js:
  - "node"
  - "6"

before_install:
  - curl https://install.meteor.com | /bin/sh
  - export PATH="$HOME/.meteor:$PATH"

services:
  - mongodb

cache:
  directories:
    - node_modules

before_script:
  - npm --version
  - meteor npm install
  - meteor --version
  - cat /home/travis/build/kevinrue/Opera/.meteor/versions

script:
  - meteor build $HOME/opera.tar.gz
  - meteor npm test
