machine:
  node:
    version: 7.5.0
dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - meteor update --release 1.4.3-rc.3
    - meteor npm install
checkout:
  post:
    - git submodule update --init
